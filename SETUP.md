# הקמה — משמרות קורטדו (אפליקציה עצמאית)

האפליקציה הזו לא תלויה ב-Claude. היא דף אינטרנט רגיל, עם כניסה של גוגל ומסד נתונים של Firebase.
ההקמה כולה בלחיצות באתר, בלי להתקין כלום. בערך 15 דקות.

---

## שלב 1 — פרויקט Firebase (5 דקות)

1. נכנסים ל-**console.firebase.google.com** ומתחברים עם `cortado.snir@gmail.com`.
2. **Create a Firebase project** ← שם: `cortado-ops` ← Continue.
3. בשאלה על Google Analytics אפשר לבחור **Disable** (לא נחוץ) ← Create project.

## שלב 2 — כניסה עם גוגל (2 דקות)

1. בתפריט השמאלי: **Build ← Authentication ← Get started**.
2. ברשימת הספקים בוחרים **Google** ← מפעילים את המתג **Enable**.
3. **Project support email:** בוחרים את המייל שלך ← **Save**.

## שלב 3 — מסד הנתונים (3 דקות)

1. בתפריט: **Build ← Firestore Database ← Create database**.
2. **Location:** `eur3 (europe-west)` ← Next.
3. בוחרים **Start in production mode** ← Create. (הכללים שלנו נכנסים בשלב 6.)

## שלב 4 — מפתחות החיבור (2 דקות)

1. לוחצים על גלגל השיניים למעלה ← **Project settings**.
2. גוללים ל-**Your apps** ← לוחצים על סמל **</>** (Web).
3. **App nickname:** `cortado-shifts` ← **Register app**. אין צורך ב-Firebase Hosting כאן.
4. מופיע קטע קוד עם `const firebaseConfig = { ... }`. **מעתיקים אותו** ושולחים לי, או ממלאים לבד:
   - פותחים את הקובץ `config.example.js`
   - שומרים אותו בשם **`config.js`**
   - מדביקים את הערכים מתוך Firebase במקומות המתאימים

> הערכים האלה גלויים בקוד הדף לכל מי שנכנס, וזה תקין ומתוכנן. האבטחה נאכפת בכללים שבשלב 6.

## שלב 5 — פרסום הדף (3 דקות)

**הדרך הפשוטה: GitHub Pages**

1. יוצרים מאגר **ציבורי** חדש בשם `cortado-app` (github.com/new).
2. מעלים אליו את כל הקבצים מהתיקייה הזו, כולל `config.js` שיצרת.
3. במאגר: **Settings ← Pages**.
4. תחת **Branch** בוחרים `main` ואת התיקייה `/ (root)` ← **Save**.
5. אחרי דקה-שתיים מופיעה הכתובת:
   `https://cortadosnir-design.github.io/cortado-app/`

**חלופה: Firebase Hosting** — דורשת התקנת כלי פיתוח במחשב. לא נחוצה בשלב הזה.

## שלב 6 — אבטחה (2 דקות)

1. **Firestore Database ← Rules**.
2. מוחקים את מה שכתוב שם, ומדביקים את כל התוכן של הקובץ `firestore.rules` ← **Publish**.
3. חוזרים ל-**Authentication ← Settings ← Authorized domains ← Add domain**
   ומוסיפים את הדומיין שקיבלת בשלב 5, למשל `cortadosnir-design.github.io`.

## שלב 7 — בדיקה

1. פותחים את הכתובת בטלפון ולוחצים **כניסה עם גוגל**.
2. מוסיפים משמרת, לוחצים **פתח לשיבוץ**, ונרשמים אליה.
3. שולחים את הקישור לעובד, והוא נכנס עם חשבון הגוגל שלו ומשתבץ.
4. בטלפון: בתפריט הדפדפן בוחרים **הוספה למסך הבית**, והאפליקציה תיראה כמו אפליקציה רגילה.

---

## מי יכול מה

| פעולה | מנהל | עובד |
|---|---|---|
| לראות משמרות | ✓ | ✓ |
| להוסיף, למחוק ולפתוח שבוע | ✓ | ✗ |
| להשתבץ ולבטל את עצמו | ✓ | ✓ (בשבוע פתוח) |
| להסיר עובד אחר ממשמרת | ✓ | ✗ |

המנהלים מוגדרים בשני מקומות, ושניהם צריכים להתעדכן יחד:
- `config.js` ← `OWNER_EMAILS` (קובע מה מוצג במסך)
- `firestore.rules` ← רשימת המיילים בפונקציה `isOwner` (קובע מה מותר בפועל)

## עלות

בהיקף של עגלת קפה הכול נכנס לתוכנית החינמית של Firebase (Spark): עד 50,000 קריאות ליום.
